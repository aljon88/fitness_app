{
  "name": "fitness-app-navigation-test",
  "version": "1.0.0",
  "description": "Navigation integration tests for AI Fitness Trainer app",
  "scripts": {
    "test": "jest",
    "test:navigation": "jest navigation_integration_test.js"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jsdom": "^22.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "setupFilesAfterEnv": ["<rootDir>/jest.setup.js"]
  }
}